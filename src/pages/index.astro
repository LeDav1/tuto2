---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import SplitText from '../components/Splitext';

import '../styles/global.css';

const pageTitle = "My astro tutorial"
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>{pageTitle}</title>
	</head>
	<body>
		<Header/>
			<h1 class="2xl bg-gradient-to-r from-purple-400 via-pink-500 to-red-500">{pageTitle}</h1>
		<!-- <SplitText
		client:only="gsap"
		text="Hello, GSAP!"
		className="text-2xl font-semibold text-center"
		delay={100}
		duration={0.6}
		ease="power3.out"
		splitType="chars"
		from={{ opacity: 0, y: 40 }}
		to={{ opacity: 1, y: 0 }}
		threshold={0.1}
		rootMargin="-100px"
		textAlign="center"
		onLetterAnimationComplete={() => { console.log("Hehehehhe")}}/> -->
		<Footer/>
		<script>
			import '../scripts/menu.js'
		</script>
	</body>
</html>
